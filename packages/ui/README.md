# @kasina/ui

**Deprecated.** This package is leftover Turborepo starter boilerplate.

The production web app (`apps/web`) uses local components under `apps/web/src/components/`. Do not add new imports from this package.

Kept temporarily so existing workspace references do not break; safe to remove once nothing depends on it.
